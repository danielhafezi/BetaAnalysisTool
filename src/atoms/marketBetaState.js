import { atom } from 'recoil';

export const marketBetaResultsState = atom({
    key: 'marketBetaResults',
    default: null,
});

export const betaPatternResultsState = atom({
    key: 'betaPatternResults',
    default: null,
}); 